name: Hello world
on: workflow_dispatch #only manually
jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: hello step
        run: |
         echo "this is line one"
         echo "this is line two"
